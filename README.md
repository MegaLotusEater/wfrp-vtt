# WFRP VTT

A local-first Warhammer Fantasy Roleplay virtual tabletop built with React and Vite.

## What It Does

- Presents a map-based game view with movable tokens
- Shows a detailed character sheet for the current player character
- Provides a chatbot-style virtual Game Master
- Uses a lightweight WFRP-style rules engine for rolls and outcomes
- Persists some UI state in the browser with `localStorage`

## Tech Stack

- React 19
- Vite
- JavaScript / JSX
- Google Gemini API client (`@google/genai`)
- ESLint

## Getting Started

### 1. Install dependencies

```powershell
npm install
```

### 2. Set up environment variables

Create a local `.env` file in the project root by copying `.env.example`.

```env
GEMINI_API_KEY=your_real_key_here
```

The browser no longer reads the Gemini key directly. The local Node proxy reads it from `.env` and the frontend talks to that proxy through `/api`.

If you still have `VITE_GEMINI_API_KEY` from the older setup, the proxy will accept it temporarily, but `GEMINI_API_KEY` is the preferred name.

### 3. Start the local API proxy

```powershell
npm run api
```

### 4. Start the dev server

```powershell
npm run dev
```

Open the local URL Vite prints, usually:

```text
http://localhost:5173
```

## Useful Scripts

- `npm run dev` - start the local development server
- `npm run api` - start the local Gemini proxy server
- `npm run build` - build the app for production
- `npm run preview` - preview a production build locally
- `npm run lint` - run ESLint

## Project Layout

- `src/` - application source code
- `public/` - static assets such as maps and textures
- `dist/` - build output generated by Vite
- `node_modules/` - installed dependencies

## Environment And Secrets

- Keep the real `.env` file out of Git.
- Commit only `.env.example` as the safe template.
- If the Gemini key has ever been shared, rotate it before using the repo in a shared remote.
- The frontend reads `/api/gm`, and the backend proxy is the only process that touches the Gemini key.
- `npm run preview` still expects the local `npm run api` server to be running so the AI GM can respond.

## Notes

- `rulebook.pdf` and the PDF extraction scripts are local working files right now.
- The app is still early-stage and intentionally simple so the rules, state, and AI layers can be improved in small steps.
